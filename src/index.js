import React from "react"; //shortcut imr
import ReactDOM from "react-dom"; //this puts all the info on the browser pg
import App from "./components/App.js";

ReactDOM.render(
  <App name="Hafeeza" />,

  document.getElementById("root")
); //renders info from App
